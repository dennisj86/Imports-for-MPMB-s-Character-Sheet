
CalcEncumbrance();

Widget Annotation Activated:

MakeButtons();

Widget Annotation Activated:

MakeIconMenu_IconOptions();


Widget Annotation Focus Out:
 

Widget Annotation Activated:
ClickClasses();

Widget Annotation Focus Out:

CalcExperienceLevel(true);


Widget Annotation Activated:

AddExperiencePoints();


Widget Annotation Focus Out:
SetHPTooltip(false, false);

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Acr');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Acr', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Ani');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Ani', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Arc');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Arc', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Ath');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Ath', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Dec');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Dec', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('His');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('His', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Ins');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Ins', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Inti');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Inti', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Inv');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Inv', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Med');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Med', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Nat');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Nat', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Perc');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Perc', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Perf');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Perf', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Pers');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Pers', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Rel');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Rel', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Sle');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Sle', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Ste');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Ste', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applySkillClick('Sur');

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
applySkillClick('Sur', true);

Widget Annotation Mouse Pressed:
calcStop();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:

MakeHPMenu_HPOptions();


Widget Annotation Activated:

HealItNow();


Widget Annotation Focus Out:
SetHPTooltip(false, false);

Widget Annotation Focus Out:
SetHPTooltip(false, false);

Widget Annotation Focus Out:
SetHPTooltip(false, false);

Widget Annotation Focus Out:
SetHPTooltip(false, false);

Widget Annotation Focus Out:
SetHPTooltip(false, false);

Widget Annotation Focus Out:
SetHPTooltip(false, false);

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
ConditionSet();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
setCheckboxProfsManual();

Widget Annotation Activated:
setCheckboxProfsManual();

Widget Annotation Activated:
setCheckboxProfsManual();

Widget Annotation Activated:
setCheckboxProfsManual();

Widget Annotation Activated:
setCheckboxProfsManual();

Widget Annotation Activated:
setCheckboxProfsManual();

Widget Annotation Activated:

if (event.target.isBoxChecked(0) === 1) {
	RemoveString("Proficiencies Remember", "lightoff");
	AddString("Proficiencies Remember", "lighton");
} else {
	RemoveString("Proficiencies Remember", "lighton");
	AddString("Proficiencies Remember", "lightoff");
}


Widget Annotation Focus Out:
setOtherWeaponProfsManual();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Focus Out:

LoadAmmo();


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Focus Out:

LoadAmmo();


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

this.delay = true;
this.calculate = false;

var LeftRight = "";
if (event.target.name.substring(0, 8) === "AmmoLeft") {
	LeftRight = "AmmoLeft";
} else if (event.target.name.substring(0, 9) === "AmmoRight") {
	LeftRight = "AmmoRight";
}
var theAmount = LeftRight + "Display.Amount";
if (LeftRight) {
	var current = LeftRight + "Display.CurrentAmount"
	
	if (event.target.isBoxChecked(0) === 1 && this.getField(theAmount).value > 0) {
		this.getField(theAmount).value -= 1;
		this.getField(current).value -= 1;
	} else if (event.target.isBoxChecked(0) === 0) {
		this.getField(theAmount).value += 1;
		this.getField(current).value += 1;
	}
	
	if (What(theAmount) < What(LeftRight + "Display.MaxAmount") && What(LeftRight + "Display.SaveAmount") <= What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo();
	} else if (What(current) < What(LeftRight + "Display.MaxAmount")) {
		LoadAmmo(What(current));
	}
}

this.calculate = IsNotReset;
this.delay = false;
if (IsNotReset) {
	this.calculateNow()
};


Widget Annotation Activated:

Toggle2ndAbilityDC();


Widget Annotation Activated:
resetLimFeaUsed('short rest');

Widget Annotation Activated:
resetLimFeaUsed('long rest');

Widget Annotation Activated:
resetLimFeaUsed('dawn');

Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:

MakeLimFeaMenu();
LimFeaOptions();


Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeActionMenu_ActionOptions();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:

LoadAmmo();


Widget Annotation Activated:

LoadAmmo();


Widget Annotation Activated:
MakeSkillsMenu_SkillsOptions();


Widget Annotation Activated:
setPlayersMakeAllRolls();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Activated:
ClassFeatureOptions();

Widget Annotation Activated:

MakeBackgroundMenu();
BackgroundOptions();

Widget Annotation Activated:

MakeRaceMenu();
RaceFeatureOptions();


Widget Annotation Activated:

MakeInventoryMenu();
InventoryOptions();

Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

WeightToCalc_Button();


Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Activated:
ApplyAttunementMI(parseFloat(event.target.name.slice(-2)));

Widget Annotation Activated:
ApplyAttunementMI(parseFloat(event.target.name.slice(-2)));

Widget Annotation Activated:
ApplyAttunementMI(parseFloat(event.target.name.slice(-2)));

Widget Annotation Activated:
ApplyAttunementMI(parseFloat(event.target.name.slice(-2)));

Widget Annotation Activated:
ApplyAttunementMI(parseFloat(event.target.name.slice(-2)));

Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:
MakeFeatMenu_FeatOptions();

Widget Annotation Activated:
MakeFeatMenu_FeatOptions();

Widget Annotation Activated:
MakeFeatMenu_FeatOptions();

Widget Annotation Activated:
MakeFeatMenu_FeatOptions();

Widget Annotation Activated:
MakeMagicItemMenu_MagicItemOptions();

Widget Annotation Activated:
MakeMagicItemMenu_MagicItemOptions();

Widget Annotation Activated:
MakeMagicItemMenu_MagicItemOptions();

Widget Annotation Activated:
MakeMagicItemMenu_MagicItemOptions();

Widget Annotation Activated:
MakeMagicItemMenu_MagicItemOptions();

Widget Annotation Activated:

MakeIconMenu_IconOptions();


Widget Annotation Activated:

MakeIconMenu_IconOptions();


Widget Annotation Activated:
MakeCompMenu_CompOptions();

Widget Annotation Focus Out:
SetHPTooltip(false, true);

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Focus Out:
SetHPTooltip(false, true);

Widget Annotation Focus Out:
SetHPTooltip(false, true);

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
applyCompSkillClick();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
setCompProfDie();

Widget Annotation Activated:
MakeHPMenu_HPOptions(false, true);

Widget Annotation Activated:

HealItNow();


Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:
SetThisFldVal();

Widget Annotation Activated:

MakeIconMenu_IconOptions();


Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Focus In:

RememberGearTempOnFocus();

Widget Annotation Focus Out:

SetGearWeightOnBlur();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:
MakeAttackLineMenu_AttackLineOptions();

Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeInventoryLineMenu();
InventoryLineOptions();


Widget Annotation Activated:

MakeNotesMenu_NotesOptions();
